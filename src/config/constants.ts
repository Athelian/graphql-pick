export const DEFAULT_OPTIONS = {};

export const PATH_SEPARATOR = ".";

export const ALIAS_DELIMITER = "__alias_";
export const ALIAS_MATCHER = new RegExp(`${ALIAS_DELIMITER}(.*?)_`);

export const FRAGMENT_DELIMITER = "__fragment_";
